import { Fragment } from 'react';
import CheckoutForm from './CheckoutForm';

const Checkout = () => {
  return (
    <Fragment>
      <CheckoutForm />
    </Fragment>
  );
};

export default Checkout;
